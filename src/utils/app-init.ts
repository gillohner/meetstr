// src/utils/app-init.ts
// Global app initialization that should run before any components
import "@/utils/formatting/dayjsConfig"; // This will configure dayjs with proper locale

// This file ensures dayjs is properly configured when imported anywhere in the app
export {};
